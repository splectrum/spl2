# AI Wishlist

Feature requests from Claude's perspective working with splectrum.

---

## Active

### spl/script API
Helper libs and REPL design. See `spl/script/_reqs/` for details:
- `helper_lib.md` - ai.js utilities (DONE)
- `repl_design.md` - interactive session plans

### Richer error messages
Fix as encountered. Example: "No input schema found" should show searched stack.

### --methods flag for whoami
Show all available methods on a container and where they come from.

---

## Resolved

- **Debug/inspection** → use inline scripts + ai.js lib
- **Stack inspection** → `ai.stack()` or whoami
