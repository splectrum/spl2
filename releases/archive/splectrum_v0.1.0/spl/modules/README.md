# spl/modules

Modules spot type definition - the root container for modules.

## Overview

Modules is a container type representing the modules spot. It extends spl/container with:

- Root level (no parent constraint)
- Children must be module containers
- Entry point for module discovery

## Extends

spl/container

## Type Constraints

See `_reqs/spl_modules_type_v1.0.0.md` for full specification.
