// spl/introspection - Introspection API type
// Base type for containers with introspection methods (whoami, report, selfeval)

export default async function(record, requireSpl) {
  // Introspection type - no direct implementation
  // Methods (whoami, report, selfeval) handle introspection
}
