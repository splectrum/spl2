# spl/module

Module type definition - what all modules must be.

## Overview

Module is a container type representing a standalone work unit. It extends spl/container with:

- Parent must be modules container
- Children must be package containers
- Standalone mycelium (self-contained, portable)

## Extends

spl/container

## Type Constraints

See `_reqs/spl_module_type_v1.0.0.md` for full specification.
