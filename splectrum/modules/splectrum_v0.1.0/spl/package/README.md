# spl/package

Package type definition - what all packages must be.

## Overview

Package is a container type that groups related APIs. It extends spl/container with:

- Parent must be module container
- Children must be API containers
- Organizational grouping for deployment

## Extends

spl/container

## Type Constraints

See `_reqs/spl_package_type_v1.0.0.md` for full specification.
