# Modules

**Type:** Folder (module root)

## Definition

Formal module bundles. Implementation layer with package/api/method hierarchy.

## Requirements

- All subfolders must be module bundles
- Each subfolder has README and entrypoint
- Each bundle follows package/api/method structure
